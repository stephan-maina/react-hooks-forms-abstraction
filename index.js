import React from 'react';
import ReactDOM from 'react-dom';
import MyForm from './MyForm'; // Replace './MyForm' with the actual path to the file.

ReactDOM.render(
  <React.StrictMode>
    <MyForm />
  </React.StrictMode>,
  document.getElementById('root') // Replace 'root' with the ID of the HTML element where you want to render the form.
);
